name: ci

on:
  push:

jobs:
  lint:
    name: ✨
    runs-on: ubuntu-latest
    steps:

    - uses: actions/checkout@v6

    - uses: actions/setup-python@v6
      with:
        python-version: "3.14"
        pip-install: -e .[lint]
        cache: pip

    - run: ruff format --diff

    - run: ruff check

